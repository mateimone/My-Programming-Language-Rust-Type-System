val mut a = &mut(vec![vec![&(Red)]]);

fun test(v: &mut &mut List<List<&Light>>) -> &Light = {
    (*(*v))[0] = vec![&(Yellow)];
    return &(Green)
}

val mut b = &(Red);
{
    set b = test(&mut a);
}

return (b == &(Red))